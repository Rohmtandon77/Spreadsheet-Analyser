# Core inference (vLLM installs torch, transformers, etc.)
vllm>=0.16.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.30.0
alembic>=1.14.0

# Job queue
redis>=5.0.0

# Data analysis (used by the sandbox)
pandas>=2.2.0
openpyxl>=3.1.0
matplotlib>=3.9.0
seaborn>=0.13.0
scipy>=1.14.0

# Voice (STT + TTS) - all local, no external APIs
faster-whisper>=1.0.0
piper-tts>=1.0.0

# CLI
click>=8.1.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.18
openai>=1.60.0
pydantic>=2.10.0
